# Foundation import checkpoint

This development-branch checkpoint records the recovered notebooks, configuration templates, documentation and repository scaffolding from the interrupted v2 import.

**Do not run this checkpoint yet.** The `vhd/` helper package, tests and repository validation tool still need to be attached in a subsequent commit. The pull request is intentionally a draft. CI will be added with the implementation, rather than running against this partial checkpoint.

The original `project_1/` notebooks and `main` branch are not modified by this checkpoint.
